# 🎯 Финальная настройка зеркалирования

## Текущая ситуация

- **Домен и хостинг**: Namecheap (vacanzabianca.com)
- **Основная аудитория**: Россия (но есть и английская)
- **Проблема**: Сайт не открывается в России без VPN

## ✅ Решение: Зеркалирование

**Основной сайт:** vacanzabianca.com (Namecheap) - для международной аудитории  
**Зеркало:** vacanzabianca.ru (Timeweb) - для России

Оба сайта будут обновляться автоматически при каждом пуше в GitHub!

## 💰 Стоимость

### Что уже есть:
- ✅ Домен .com на Namecheap (уже оплачен)
- ✅ Хостинг на Namecheap (уже оплачен)

### Что нужно добавить:
1. **Хостинг Timeweb** - 99 руб/мес (~1 евро/мес) = **~12 евро/год**
2. **Домен .ru** - 179 руб/год (~1.8 евро/год)

**Итого дополнительно: ~13.8 евро/год**

## 📋 Пошаговая инструкция

### Шаг 1: Купить домен .ru на Timeweb (5 минут)

1. В панели Timeweb найдите и купите `vacanzabianca.ru` за 179 руб
2. Опционально: добавьте `вацанзабианца.рф` за 99 руб (со скидкой)

### Шаг 2: Настроить хостинг на Timeweb (10 минут)

1. В панели Timeweb создайте сайт
2. Привяжите домен `vacanzabianca.ru`
3. Получите FTP данные:
   - Хост: `ftp.timeweb.ru` (или IP адрес)
   - Логин: ваш логин на Timeweb
   - Пароль: ваш пароль
   - Путь: `/public_html` (обычно)

### Шаг 3: Настроить автоматический деплой на Timeweb (5 минут)

1. Перейдите в GitHub репозиторий
2. Settings → Secrets and variables → Actions
3. Добавьте секреты:

```
TIMEWEB_FTP_HOST = ftp.timeweb.ru (или IP, который даст Timeweb)
TIMEWEB_FTP_USER = ваш_логин_на_timeweb
TIMEWEB_FTP_PASS = ваш_пароль_на_timeweb
TIMEWEB_FTP_PATH = /public_html
```

### Шаг 4: Настроить автоматический деплой на Namecheap (если еще не настроен)

**Вариант А: Через GitHub Actions (рекомендуется)**

Создайте файл `.github/workflows/deploy-namecheap.yml`:

```yaml
name: Deploy to Namecheap

on:
  push:
    branches:
      - main

jobs:
  deploy-to-namecheap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.NAMECHEAP_FTP_HOST }}
          username: ${{ secrets.NAMECHEAP_FTP_USER }}
          password: ${{ secrets.NAMECHEAP_FTP_PASS }}
          local-dir: ./dist/
          server-dir: ${{ secrets.NAMECHEAP_FTP_PATH }}
```

Добавьте секреты в GitHub:
- `NAMECHEAP_FTP_HOST`
- `NAMECHEAP_FTP_USER`
- `NAMECHEAP_FTP_PASS`
- `NAMECHEAP_FTP_PATH`

**Вариант Б: Через Vercel (если используете)**

Если сайт деплоится через Vercel, то:
1. Vercel автоматически деплоит при пуше в GitHub
2. Настройте домен vacanzabianca.com в Vercel
3. Настройте DNS на Namecheap для работы с Vercel

### Шаг 5: Проверка

1. Сделайте любой коммит и пуш
2. Проверьте, что оба сайта обновились:
   - vacanzabianca.com (Namecheap/Vercel)
   - vacanzabianca.ru (Timeweb)
3. Проверьте доступность из России

## 🔄 Как это работает

```
GitHub Push → GitHub Actions → {
  ├─→ Namecheap/Vercel (vacanzabianca.com) - для международной аудитории
  └─→ Timeweb FTP (vacanzabianca.ru) - для России
}
```

**Оба сайта синхронизируются автоматически!**

## 🌍 Настройка редиректов (опционально)

Можно настроить автоматическое определение страны и редирект:
- Пользователи из России → vacanzabianca.ru
- Остальные → vacanzabianca.com

Но это не обязательно - можно просто дать пользователям выбор.

## ✅ Преимущества этого решения

1. **Доступность для всех** - сайт работает и в России, и за рубежом
2. **Автоматическая синхронизация** - оба сайта обновляются одновременно
3. **Резерв** - если один хостинг недоступен, работает другой
4. **Низкая стоимость** - дополнительно только ~14 евро/год

## 📞 Поддержка

Если возникнут вопросы:
- Timeweb поддержка: чат на сайте
- Namecheap поддержка: через панель управления
- GitHub Actions: документация в репозитории

